<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.dam.safebar">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SafeBar">
        <activity android:name=".SignUpRest"
                android:theme="@style/Theme.SafeBar.NoStatusBar"/>
        <activity android:name=".LogInRest"
            android:theme="@style/Theme.SafeBar.NoStatusBar"/>
        <activity android:name=".ReservasRest" />
        <activity android:name=".PerfilRest" />
        <activity android:name=".Rest"
            android:theme="@style/Theme.SafeBar.NoActionBar"/>
        <activity android:name=".Cuenta"
            android:theme="@style/Theme.SafeBar.NoActionBar"/>

        <activity android:name=".ReservasUsu" />
        <activity android:name=".Buscar" />
        <activity android:name=".Inicio"
            android:theme="@style/Theme.SafeBar.NoActionBar"/>
        <activity
            android:name=".SignUp"
            android:theme="@style/Theme.SafeBar.NoStatusBar" />
        <activity
            android:name=".LogIn"
            android:theme="@style/Theme.SafeBar.NoStatusBar" >
        </activity>
        <activity android:name=".Splash"
            android:theme="@style/Theme.SafeBar.NoStatusBar.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>